var facebookController = require('../controllers/facebook');

module.exports = function (app) {
    app.get('/facebook/:id', function (request, response, next) {
        //facebookController
    });
}